// Main file the rest are just objects that are instantiated and disposed
public class Interpreter {
	/*
		2 possible options
		if it is just launched, then we launch into an interactive interpreter
		if it is launched with a valid Maple file, then it is interpreted and run
	 */
	public static void main (String ... args) {

	}
}
